[package]
name = "linkcell"
version = "0.2.0"
edition = "2021"
rust-version = "1.70"
description = "Periodic linked-cell k-nearest neighbour search for molecular simulations"
license = "MIT"
repository = "https://github.com/HaoZeke/linkcell"
homepage = "https://github.com/HaoZeke/linkcell"
documentation = "https://docs.rs/linkcell"
keywords = ["molecular-dynamics", "neighbours", "periodic", "cell-list"]
categories = ["science", "algorithms"]
readme = "README.md"

[lib]
name = "linkcell"
crate-type = ["rlib", "staticlib", "cdylib"]

[features]
default = ["capi", "parallel"]
capi = []
parallel = ["dep:rayon"]

[dependencies]
rayon = { version = "1.10", optional = true }

[[test]]
name = "abi"
required-features = ["capi"]

[profile.release]
lto = true
codegen-units = 1
