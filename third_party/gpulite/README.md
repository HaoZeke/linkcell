# gpulite (vendored header)

Pinned to `rubber-duck-debug/gpu-lite`
`1e90edb6d573289a7678c18d25b5dbf62f374abe`, the same revision as
`subprojects/gpulite.wrap`. MIT. The maturin extension compiles the
device walk against this header; CUDA is loaded at runtime.
